# Expo HAS CHANGED

This app is pinned to Expo SDK 54 (Expo Go compatible — do not upgrade without approval).
Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
